package com.sample.aijobassistant.ui.theme

import androidx.compose.ui.graphics.Color

// A deliberate choice away from default Material purple: a calm slate base
// with a warm amber accent for the score/CTA moments. Slate reads as
// professional/analytical (fitting a tool that evaluates your resume),
// amber draws the eye to score and action without feeling alarmist (avoided
// red/green as the primary accent since match scores aren't pass/fail).
val SlateDark = Color(0xFF1C2128)
val SlateMedium = Color(0xFF2D333B)
val SlateLight = Color(0xFFF4F5F7)
val Amber = Color(0xFFE8A23D)
val AmberDark = Color(0xFFB8791F)

val ScoreStrong = Color(0xFF3E8E5C) // used only for numeric score emphasis, not as a UI accent
val ScoreModerate = Color(0xFFCB9A2E)
val ScoreWeak = Color(0xFFC1543C)

val OnSlateDark = Color(0xFFE9EBEF)
val OnSlateLight = Color(0xFF1C2128)
